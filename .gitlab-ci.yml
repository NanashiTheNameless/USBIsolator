stages:
  - test

image-optimizer-manual-build-job-0:
  stage: test
  image: node:20-bookworm
  rules:
    - if: "$CI_PIPELINE_SOURCE == \"web\""
    - when: never
  script:
    - |
      echo "Image optimizer action not auto-converted; manual review required."
    - |
      echo "GitLab MR automation not auto-converted; manual review required."

image-optimizer-build-job-0:
  stage: test
  image: node:20-bookworm
  rules:
    - if: "$CI_PIPELINE_SOURCE == \"merge_request_event\""
    - if: "$CI_PIPELINE_SOURCE == \"push\""
    - if: "$CI_PIPELINE_SOURCE == \"merge_request_event\""
    - when: never
  script:
    - |
      echo "Image optimizer action not auto-converted; manual review required."
